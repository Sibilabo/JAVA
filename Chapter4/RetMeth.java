//Wprowadza metodę zwracającą wartość
/*
class Vehicle {
    int passengers; // Liczba pasażerów
    int fuelcap; // Pojemność zbiornika paliwa w litrach
    double lkm; // Zużycie paliwa w litrach na 100km

    //Zwraca zasięg
    int range() { 
        return (int) (fuelcap / lkm * 100);
    }
}

class RetMeth {
    public static void main(String args[]) {
        Vehicle minivan = new Vehicle();
        Vehicle sportscar = new Vehicle();

        int range1, range2;

        // Przypisuje wartości składowym obiektu minivan
        minivan.passengers = 7;
        minivan.fuelcap = 65;
        minivan.lkm = 9.1;

        // Przypisuje wartości składowym obiektu sportscar
        sportscar.passengers = 2;
        sportscar.fuelcap = 55;
        sportscar.lkm = 12.5;

        //Ustala zasięg

        range1 = minivan.range();
        range2 = minivan.range();


        System.out.print("Minivan przewozi " + minivan.passengers + 
                            " osób na odległość do " + range1 + "kilometrów.");

        System.out.print("Auto sportowe przewozi " + sportscar.passengers + 
                            " osoby na odległość do " + range2 + " kilometrów.");
    }
}
*/